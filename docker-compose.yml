version: '3.8'

services:
  mysqlDb:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=foyer
    ports:
      - 3306:3306
    volumes:
      - mysql-data:/var/lib/mysql       # Volume for MySQL data
      - mysql-logs:/var/log/mysql       # Volume for MySQL logs

  FoyerApplication:
    depends_on:
      - mysqlDb
    image: ranimtlili/foyer-app        # Use the provided Docker image
    restart: on-failure
    ports:
      - 8082:8080                      # Map Spring Boot's default port to host port 8082
    environment:
      SPRING_APPLICATION_JSON: '{
        "spring.datasource.url": "jdbc:mysql://mysqlDb:3306/foyer?createDatabaseIfNotExist=true",
        "spring.datasource.username": "root",
        "spring.datasource.password": null,
        "spring.jpa.properties.hibernate.dialect": "org.hibernate.dialect.MySQL5InnoDBDialect",
        "spring.jpa.hibernate.ddl-auto": "update"
      }'
    stdin_open: true
    tty: true

volumes:
  mysql-data:
  mysql-logs:
